from tkinter import *

window = Tk()  # instantiate an instance of a window
window.geometry("400x400")
window.title("Heat Baba GUI")

# icon = PhotoImage(file='qrcode_chrome.png')
# window.iconphoto(True, icon)
window.config(background="#5cfcff")

window.mainloop()  # place a window on the computer screen, listen for events
