# db = lambda x:x * 10
# mult = lambda x, y: x*y
# # print(mult(12, 2))

# add = lambda w, y, u: w * y + u
# print(add(10, 32, 98))

# full_name = lambda first_name, last_name: first_name+ " "+ last_name
# print(full_name("Yaw", "Mensah"))


age_chhecck = lambda age: True if age >= 19 else False
print(age_chhecck(12))